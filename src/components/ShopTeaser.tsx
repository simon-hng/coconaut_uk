import * as React from "react";
import SectionTwoCol from "./global/SectionTwoCol";

const Text = <>Todo: get data from shopify</>;
const Visual = <>todo</>;

const ShopTeaser = () => <SectionTwoCol visual={Visual} text={Text} />;

export default ShopTeaser;
